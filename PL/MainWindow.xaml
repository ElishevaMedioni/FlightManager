<Window x:Class="PL.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PL"
        xmlns:helper="clr-namespace:BE;assembly=BE"
        xmlns:system="clr-namespace:System;assembly=mscorlib"
        xmlns:converters="clr-namespace:PL.Converters"
        xmlns:m="clr-namespace:Microsoft.Maps.MapControl.WPF;assembly=Microsoft.Maps.MapControl.WPF"
        mc:Ignorable="d"
        Title="MainWindow" Height="550" Width="1213">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="50*"/>
            <RowDefinition Height="469*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="77*"/>
            <ColumnDefinition Width="160*"/>
            <ColumnDefinition Width="47*"/>
            <ColumnDefinition Width="929*"/>
        </Grid.ColumnDefinitions>


        <!--<Button Content="Start Track" HorizontalAlignment="Left" Height="33" Margin="646,10,0,0" VerticalAlignment="Top" Width="126" Click="Button_Click_1" Grid.Column="2"/>-->


        <UserControl x:Name="MainUC"  HorizontalAlignment="Left" Margin="35,0,0,0" VerticalAlignment="Top" Width="1112" Height="557" Grid.ColumnSpan="3" Grid.RowSpan="2" Grid.Column="1" >
            <UserControl.Resources>
                <ResourceDictionary>
                    <Style TargetType="m:Pushpin" x:Key="FromIsrael">
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate>
                                    <Image Source="C:\Users\zeevm\source\repos\FlightManager\PL\Images\airplaneUpLeft.png"  />
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>

                    </Style>
                    <Style TargetType="m:Pushpin" x:Key="ToIsrael">
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate>
                                    <Image Source="C:\Users\zeevm\source\repos\FlightManager\PL\Images\airplaneUpRight.png"/>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </ResourceDictionary>
            </UserControl.Resources>

        </UserControl>
        <StackPanel x:Name="menuUC" Margin="10,10,130,210" Grid.ColumnSpan="2" Grid.Row="1">
            <Button Content="Flights" Height="60" x:Name="FlightsButton" Click="FlightsButton_Click">
                <Button.Resources>
                    <Style TargetType="Border">
                        <Setter Property="CornerRadius" Value="5"/>
                    </Style>
                </Button.Resources>
            </Button>
            <Button Content="Historic" Height="60" x:Name="HistoricButton" Click="historicButton_Click">
                <Button.Resources>
                    <Style TargetType="Border">
                        <Setter Property="CornerRadius" Value="5"/>
                    </Style>
                </Button.Resources>
            </Button>

            <Button Content="Exit" Height="60" x:Name="ExitButton" Click="ExitButton_Click" VerticalAlignment="Bottom">
                <Button.Resources>
                    <Style TargetType="Border">
                        <Setter Property="CornerRadius" Value="5"/>
                    </Style>
                </Button.Resources>
            </Button>






        </StackPanel>


    </Grid>
</Window>
