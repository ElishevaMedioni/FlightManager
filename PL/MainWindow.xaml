<Window x:Class="PL.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
        xmlns:converters="clr-namespace:PL.Converters"
        xmlns:helper="clr-namespace:BE;assembly=BE"
        xmlns:system="clr-namespace:System;assembly=mscorlib"
        xmlns:m="clr-namespace:Microsoft.Maps.MapControl.WPF;assembly=Microsoft.Maps.MapControl.WPF"
        
        xmlns:local="clr-namespace:PL"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Window.Resources>
        <Style TargetType="m:Pushpin" x:Key="FromIsrael">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate>
                        <Image Source="images\airplaneUpRight.png"/>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="m:Pushpin" x:Key="ToIsrael">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate>
                        <Image Source="images\airplaneUpLeft.png"/>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <ObjectDataProvider ObjectType="{x:Type helper:HelperClass}" MethodName="GetTimeFromEpoch" x:Key="EpochConvert">
            <ObjectDataProvider.MethodParameters>
                <system:Double>0</system:Double>
            </ObjectDataProvider.MethodParameters>
        </ObjectDataProvider>

        <converters:TickToDateTimeConverter x:Key="TickConvert"/>

    </Window.Resources>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="397*"/>
            <ColumnDefinition Width="403*"/>
        </Grid.ColumnDefinitions>

        <!--<Grid.RowDefinitions>
            <RowDefinition Height="50*"/>
            <RowDefinition Height="469*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="222*"/>
            <ColumnDefinition Width="221*"/>
            <ColumnDefinition Width="1449*"/>
        </Grid.ColumnDefinitions>-->


        <Grid Grid.ColumnSpan="2">
            <m:Map x:Name="myMap" CredentialsProvider="AnYVK6ttYXZfDmVxGW1VPgMPPMtcznBW2icQCtOR3QEjPkBqv5ONWvQFbk2XQE0K" Margin="377,49,10,10" Center="32.000454, 34.870743" AnimationLevel="None" ZoomLevel="5"/>
        </Grid>

        <Label Content="Flight Details" HorizontalAlignment="Left" Margin="261,18,0,0" VerticalAlignment="Top" Height="26" Width="79"/>

        <ListBox ItemsSource="{Binding}" x:Name="InFlightsListBox" HorizontalAlignment="Left" Height="112" Margin="30,67,0,0" VerticalAlignment="Top" Width="176" SelectionChanged="FlightsListBox_SelectionChanged">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="{Binding SourceFilter}"/>
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>


        <Label Content="Incoming" HorizontalAlignment="Left" Margin="30,36,0,0" VerticalAlignment="Top" Height="26" Width="60"/>
        <Label Content="Outgoing" HorizontalAlignment="Left" Margin="30,0,0,0" VerticalAlignment="Center" Height="26" Width="60"/>


        <!--<Button Content="Start Track" HorizontalAlignment="Left" Height="33" Margin="607,10,0,0" VerticalAlignment="Top" Width="126" Click="Button_Click_1" RenderTransformOrigin="0.286,0.394"/>-->
        <TextBox x:Name="Counter" HorizontalAlignment="Left" Margin="361,17,0,378" TextWrapping="Wrap" Text="1" Width="26" Grid.Column="1" />

        <StackPanel x:Name="DetailsPanel" HorizontalAlignment="Left" Height="218" VerticalAlignment="Top" Width="147" Margin="222,58,0,0">
            <TextBlock Text="{Binding identification.callsign}"/>
            <TextBlock Text="{Binding aircraft.registration}"/>
            <TextBlock Text="{Binding airline.name}"/>
            <TextBlock Text="{Binding airport.destination.name}"/>
            <TextBlock Text="{Binding airport.destination.position.country.name}"/>
            <TextBlock Text="{Binding airport.origin.name}"/>
            <TextBlock Text="{Binding airport.origin.position.country.name}"/>
            <TextBlock Text="{Binding status.text}"/>
            <TextBlock Text="{Binding time.estimated.arrival, Converter={StaticResource TickConvert}}"/>

        </StackPanel>

        <Button Content="Read All" HorizontalAlignment="Left" Height="36" Margin="10,0,0,0" VerticalAlignment="Top" Width="89" Click="Button_Click"/>
        <ListBox ItemsSource="{Binding}" Name="OutFlightsListBox" HorizontalAlignment="Left" Height="112" Width="176" Margin="30,235,0,0" VerticalAlignment="Top">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="{Binding Destination}"/>
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>



        </ListBox>


    </Grid>

</Window>
